{
  "name": "nurexia",
  "version": "0.1.0",
  "description": "Nurexia - Terminal Based CLI Application with improved multilingual support",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "start": "pnpm --filter nurexia-cli start",
    "clean": "rm -rf packages/*/dist",
    "setup": "pnpm install && pnpm build"
  },
  "keywords": ["cli", "tui", "terminal", "blessed", "multilingual", "korean"],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
